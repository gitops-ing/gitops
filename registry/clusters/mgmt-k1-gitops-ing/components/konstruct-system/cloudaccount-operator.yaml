apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: development-cloudaccount-operator
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
  annotations:
    argocd.argoproj.io/sync-wave: '0'
spec:
  project: default
  source:
    repoURL: europe-west2-docker.pkg.dev/civo-com/charts
    chart: cloudaccount-operator
    targetRevision: 0.1.0-rc.e749cf8c
    helm:
      values: |-
        imagePullSecrets:
          - name: docker-config
      parameters: []
  destination:
    name: in-cluster
    namespace: konstruct-system
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
